// src/components/ui/toaster.tsx
// Minimal toast placeholder — thay bằng shadcn toast sau khi chạy `npx shadcn@latest add toast`
"use client";

export function Toaster() {
  return null; // Shadcn toast sẽ render ở đây sau khi cài
}
